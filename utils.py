# People per product
def people_per_product(population_density, product_density):
  return population_density/product_density

# visibility given perimeter and visible perimeter
def cashier_visibility(total_perimeter, visible_perimeter)
  return visible / total

#People per product metric
def people_per_product_metric(population_density, product_density, max_ratio):
  return population_density/(product_density*max_ratio)

